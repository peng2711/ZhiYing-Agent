name: zhiying-agent

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  chromadb:
    image: chromadb/chroma:0.5.23
    ports:
      - "8001:8000"
    environment:
      IS_PERSISTENT: "TRUE"
      PERSIST_DIRECTORY: /chroma/chroma
      ANONYMIZED_TELEMETRY: "FALSE"
    volumes:
      - chroma-data:/chroma/chroma
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/v1/heartbeat"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s

  backend:
    build:
      context: ./backend
      target: production
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    environment:
      REDIS_URL: redis://redis:6379/0
      CHROMA_HOST: chromadb
      CHROMA_PORT: "8000"
      CHROMA_PERSIST_DIRECTORY: /app/data/chroma
      BUSINESS_DB_PATH: /app/data/business/business.db
      EVAL_BASELINE_PATH: /app/data/eval/baseline.json
      ZHIYING_SKILLS_DIR: /app/skills
    volumes:
      - backend-data:/app/data
      - ./backend/skills:/app/skills:ro
    depends_on:
      redis:
        condition: service_healthy
      chromadb:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    # 本地 Docker 演示由同源 Nginx/BFF 注入 API Key，避免把密钥编译进浏览器 JS。
    # 生产环境应为代理单独配置最小权限凭据。
    env_file:
      - ./backend/.env
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  redis-data:
  chroma-data:
  backend-data:
