# EchoMind 智能客服项目面经

本文档用于整理 EchoMind 智能客服项目的面试问答、项目介绍、核心设计、工程难点与实践经验。

## 项目概述

待补充。

## 面试问答

### 1. 为什么不直接把用户问题丢给大模型回答？你设计这一套 Agent 架构，优势在哪里？

如果只是做一个简单 Demo，直接把用户问题丢给大模型确实可以。但 EchoMind 这个项目的目标不是做一个普通聊天框，而是做一个更接近真实业务的智能客服系统。客服场景里，回答不能只靠模型常识，还要结合企业知识库、用户历史上下文、业务处理规范，以及不同问题类型对应的专业 Agent。

直接调用大模型主要有几个问题：

1. **缺少业务知识约束**  
   客服回复经常涉及退款政策、配送规则、账户安全、发票处理这些业务事实。如果直接让模型回答，它可能会凭通用知识生成一个看似合理但不符合企业规则的答案。所以项目里把 ChromaDB 知识库接入主对话链路，让 Agent 回复优先依据知识库内容。

2. **缺少多轮上下文**  
   用户不会每次都把背景说完整，可能前面已经说过订单、产品、问题类型，后面只说“那这个还能退吗”。所以项目里用了 Redis 保存当前会话工作记忆，用 ChromaDB 保存用户画像和相关历史，让回复能结合上下文，而不是每一轮都当成新问题。

3. **不同问题需要不同处理规范**  
   技术问题、账单退款、普通咨询的处理方式不一样。比如技术 Agent 要更关注错误信息和排查步骤，账单 Agent 要更关注退款审核、发票和敏感信息保护。如果只用一个通用 prompt，规则容易混在一起。所以我拆成通用、技术、账单等 Agent，再按意图路由。

4. **缺少可控的降级机制**  
   真实系统里知识库可能查不到，工具可能超时，某个 Agent 可能失败。直接调用大模型时，这些异常很难统一处理。项目里把工具调用封装起来，做了参数校验、缓存、超时、熔断和 fallback；专属 Agent 失败时也可以降级到通用 Agent。

5. **缺少评测和持续优化闭环**  
   大模型项目不能只看“这次回答像不像”。EchoMind 里做了端到端评测，真实调用 Agent 编排链路，再用 LLM-as-Judge 从相关性、准确性、完整性、有用性几个维度评分，也能发现修改 prompt 或路由逻辑后的回归问题。

这套 Agent 架构的优势在于，它把大模型从“自由回答者”变成了“受业务上下文约束的处理单元”。系统先识别用户意图，再检索知识库和记忆上下文，然后路由到合适的 Agent，并按对应 Skills 注入业务规范。这样既保留了大模型的理解和生成能力，又让回答更符合企业客服场景。

所以我不会把 EchoMind 设计成单一 Chatbot，而是设计成“意图识别 + RAG 知识库 + 多 Agent 路由 + 记忆管理 + Skills 规则注入 + 监控评测”的完整客服系统。这样更适合真实业务，因为它可控、可降级、可评估，也方便后续持续优化。

### 2. 你提到接入了知识库工具，具体是怎么检索的？拉取什么内容？

我这里接入的不是让大模型直接读一堆文档，而是把 ChromaDB 知识库封装成一个 MCP 工具，主对话链路在需要时自动检索知识库，再把相关片段拼进 Agent 上下文。这样模型回答退款、配送、账户安全这类问题时，不是凭常识发挥，而是优先依据项目里的业务文档。

整体流程是：

1. **先做意图识别和查询改写**  
   用户的问题表达可能很口语，比如“这个还能退吗”“发票怎么开”“账号登不上怎么办”。系统会先判断大致意图，再把原始问题改写成多个搜索角度，提高召回率。

2. **并行召回知识库片段**  
   每个子查询都会去 ChromaDB 的 `knowledge_base` 里做语义检索，召回相关文档片段。比如退款问题可能召回退款政策、售后流程、人工审核规则等多个片段。

3. **合并去重和重排**  
   多个子查询可能召回重复内容，所以会先合并去重。然后再让模型对召回结果按相关性重排，最后只取 Top-K 片段放进上下文，避免把无关文档全部塞给 Agent。

知识库里主要拉取几类内容：

1. **业务规则类文档**  
   比如退款政策、配送说明、会员积分、发票规则、账户安全规范。这类内容决定 Agent 回答时能不能符合企业规则。

2. **问题处理流程**  
   比如用户要退款时需要收集哪些信息，哪些情况需要人工审核，技术问题要先确认哪些错误信息。这些内容和 Skills 配合，约束 Agent 的处理步骤。

3. **常见问题答案**  
   比如订单查询、登录失败、密码重置、配送时效、会员权益等。用户问到这些问题时，系统可以直接召回对应知识片段。

4. **安全和边界规则**  
   比如不能索取完整银行卡号、不能直接承诺退款到账、涉及账户安全要引导用户走验证流程。这些规则可以减少模型生成不合规回复。

面试时我会强调两个设计点：

第一，知识库检索是**按需触发**的。不是每一轮都强行检索，而是对退款、配送、账户、技术支持等需要业务依据的问题才检索，避免不必要的延迟和上下文噪声。

第二，检索结果不是直接等于最终答案。它只是给 Agent 提供依据，最终还要结合用户当前问题、最近对话、用户画像和对应 Agent 的 Skills 生成回复。

举个例子，如果用户问“我这个订单还能退款吗”，系统会先召回退款政策和售后规则，再交给账单 Agent 结合用户问题生成回复。这样回答会更稳，不会出现模型随口承诺“可以立即退款”这种风险。

### 5. 你说系统只输出一个主意图，那什么时候会触发多 Agent？

我这里的设计是，意图识别阶段只判断一个主意图，但后面的 Agent 调度不会只依赖这一个结果。主意图解决的是“这句话最主要是什么问题”，多 Agent 解决的是“这句话里是不是同时包含多个专业领域的问题”。

比如用户说：“我登录一直报 500，而且刚才会员费还被扣了两次。”如果只看主意图，系统可能会判断这是技术问题，然后只交给技术 Agent。但这句话里其实还有一个账单问题。如果只让技术 Agent 回答，就会漏掉重复扣款这一部分。

所以我在主意图识别之后，又做了一层复合问题判断。它会看这句话里是否同时出现多个领域的信号，比如技术侧的“报错、500、无法登录、接口异常”，以及账单侧的“扣款、退款、发票、订阅、支付”等。如果只命中一个领域，就走单 Agent；如果同时命中两个领域，就触发多 Agent 并行。

举个例子：

用户说：“接口一直 500，但是我这边订阅费用已经扣了两次。”  
系统会让技术 Agent 负责分析 500、接口、日志、配置这些问题；同时让账单 Agent 负责处理订阅、扣款、退款审核这些问题。两个 Agent 各自生成自己专业范围内的回答，最后由系统把结果合并成一个回复给用户。

我这样设计的原因是，不希望意图识别变成一个很复杂的多标签分类。多标签分类在面试或者线上评估时会更难控制，也更容易出现边界不清的问题。我的做法是：主意图保持单一，保证基础路由稳定；复杂问题放到调度层处理，通过领域关键词和上下文判断是否需要协作。

所以总结一下就是：系统输出一个主意图不代表只能调用一个 Agent。主意图决定默认处理方向，复合领域判断决定是否触发多 Agent。只要用户的问题同时覆盖多个专业领域，比如“技术故障 + 账单退款”，就会触发多 Agent 并行处理。

### 6. 你的意图识别只有三种分类吗？意图识别输出主意图，为什么还命中关键词来触发多 Agent？

不是只有三种分类。这里容易混淆的是“意图类别”和“Agent 类型”。

我的意图识别本身不是只分成技术、账单、通用三类，而是会识别更细的用户意图，比如查询、投诉、请求操作、问候、转人工、技术问题、账单退款、账户管理、反馈和其他。也就是说，意图识别关注的是用户这句话的业务含义。

但是 Agent 类型没有设计得那么细，目前主要是通用 Agent、技术 Agent、账单 Agent 这几类。原因是 Agent 代表的是处理能力和处理规范，不是每个意图都需要单独一个 Agent。比如“查询订单状态”“普通咨询”“用户反馈”可以由通用 Agent 处理；“退款、扣款、发票、账户资料”更适合账单 Agent；“报错、登录失败、接口异常、部署问题”更适合技术 Agent。

所以流程上是先识别一个主意图，再把这个主意图映射到最合适的 Agent。比如识别为技术问题，就默认走技术 Agent；识别为账单或账户问题，就默认走账单 Agent；如果是普通查询或问候，就走通用 Agent。

至于为什么主意图已经有了，还要通过关键词触发多 Agent，是因为这两个判断解决的问题不一样。

主意图解决的是“这句话最主要的问题是什么”。它通常只输出一个结果，这样路由稳定，也方便统计准确率。但是用户的表达不一定只包含一个问题。比如他说：“我登录一直报错，而且刚才还被重复扣款了。”这句话主意图可能会被判断成技术问题，因为“登录报错”更明显。但“重复扣款”也是一个必须处理的账单问题。

如果完全依赖主意图，就会出现一个问题：系统只能选一个 Agent，另一个领域的问题可能被忽略。所以我额外加了领域关键词判断，它不是为了替代意图识别，而是作为补充召回，用来发现一句话里有没有第二个专业领域。

我对它的定位是这样的：

主意图识别负责“主路由”，决定默认交给谁处理。  
关键词命中负责“补充协作”，判断要不要再拉一个 Agent 一起处理。

这样设计比较工程化。因为多 Agent 触发宁可通过明确领域信号来做补充判断，而不是强行让意图识别输出多个标签。多标签意图识别虽然看起来更完整，但线上会更难评估，边界也更模糊，比如一句话里出现“退款”到底是主诉求还是背景信息，很容易影响主路由。

所以我保留单主意图，是为了让基础路由稳定；再用关键词和上下文做复合问题检测，是为了避免跨领域问题漏处理。它们不是重复逻辑，而是一个负责判断主方向，一个负责补齐协作范围。

### 7. 如果记忆出错了怎么办？

我会把“记忆出错”分成两类来看：一类是记忆组件本身不可用，比如 Redis 或 ChromaDB 连接失败；另一类是记忆内容不准确，比如摘要提炼错了、召回了不相关的历史，或者拿到了过期上下文。

第一种情况，记忆组件不可用时，不能影响主链路。因为对话系统最核心的是先能回答用户问题，记忆是增强能力，不应该变成强依赖。所以我的处理思路是：Redis 读不到最近对话，就退化成无历史上下文的一轮请求；ChromaDB 检索失败，就返回空的相关历史；用户画像更新失败，也只记录日志，不阻塞当前响应。

也就是说，记忆失败时系统会降级成“无记忆模式”，但不会直接让用户请求失败。尤其在智能客服场景里，业务知识库、当前用户问题和本轮对话是主依据，历史记忆更多是辅助上下文，不应该成为回答用户问题的唯一依据。

第二种情况是记忆内容不准。这个更危险，因为它不是明显报错，而是可能误导 Agent。我的设计原则是：记忆只能作为背景信息，不能作为最终事实来源。比如历史摘要里说“用户之前咨询过退款”，这只能提示 Agent 关注退款相关上下文，但最终回复还是要以当前问题和知识库里的业务规则为准。

如果发现记忆和当前业务规则冲突，优先相信当前知识库和用户本轮明确表达的信息。比如历史记忆里说用户之前想退款，但这次用户是在问发票问题，那就不能强行围绕退款回答。

另外我会做几层保护：

1. **隔离记忆范围**  
   记忆按 `user_id` 和 `conv_id` 区分，避免不同用户、不同会话的上下文串在一起。

2. **控制记忆长度**  
   只保留最近几轮原始对话，旧内容压缩成摘要，避免把大量低价值历史都塞给模型。

3. **记忆检索只取 Top-K**  
   相关历史不会无限召回，只取最相关的几条，减少噪声。

4. **把记忆标记成背景，不当作事实来源**  
   在 prompt 里记忆是背景信息，真正涉及退款政策、配送规则、账户安全这类业务事实时，优先依据知识库和当前用户输入。

5. **失败可观测**  
   Redis、ChromaDB、画像更新、历史检索这些失败都应该打日志，后续接入监控告警，避免长期静默失败。

如果面试官继续追问“摘要错了怎么办”，我会说摘要本身一定有信息损失，所以它只适合做历史线索，不适合做强事实依据。对于退款、账单、账户安全这类容易产生风险的问题，系统必须重新参考知识库规则；对于用户明确纠正的信息，也应该以本轮用户表达为准，并在后续压缩时把纠正后的事实写入新的摘要。

所以总结一下：**记忆出错时，系统先保证主链路可用；记忆不准时，当前问题和知识库优先；记忆只是辅助上下文，不是最终判断依据。**

### 8. 开发过程当中最难的点是什么？

我觉得最难的点不是单独接一个大模型接口，而是怎么把大模型这种不稳定、不可完全预测的能力，包装成一个相对可控的智能客服系统。

如果只是做一个 Demo，其实用户问问题，然后把问题丢给模型回答就可以了。但 EchoMind 想做的是一个完整的客服 Agent 系统，所以它不能只“会回答”，还要知道用户到底是在咨询、投诉、申请退款、反馈问题，什么时候查知识库，什么时候走技术 Agent，什么时候走账单或通用 Agent，工具失败了怎么办，历史上下文要不要用，最后回答是否符合业务规则。

开发里最难的是这几块之间的协同。

第一是上下文组织比较难。项目里有 Redis 里的当前会话记忆，有 ChromaDB 里的知识库和用户画像，还有动态 Skills 规则，再加上用户当前问题。它们都可能对回答有帮助，但不能全部无脑塞给模型，否则上下文会很长，而且容易把模型带偏。所以我做了分层：最近对话放在工作记忆里，长对话压缩成摘要，知识库只在需要时检索 Top-K，Skills 只按 Agent 类型和关键词注入。这样既保留上下文，又尽量控制噪声。

第二是知识库检索的可靠性。客服系统不能只靠模型常识回答退款政策、配送规则、账户安全这些问题，否则很容易编错业务规则。所以我把知识库检索接进主对话链路，并且在工具层做了参数校验、超时、缓存、熔断和 fallback。比如 ChromaDB 检索失败时，主对话还能继续，只是降级成没有知识库增强的回答，而不是整个接口失败。

第三是多 Agent 路由边界。项目里不是所有问题都交给一个 Agent，而是根据意图路由到通用、技术、账单等不同 Agent。这里难点在于既要保证路由稳定，又要处理复合问题。比如“我一直无法登录，而且刚才还被重复扣款”这种问题，如果只走技术 Agent 会漏掉账单部分。所以我保留单主意图做默认路由，再用领域信号判断是否需要多 Agent 协作。

第四是工程闭环。很多大模型项目停留在“能生成回答”，但上线后更重要的是可观测和可评估。所以项目里还做了 Monitor，采集 Agent 和工具的成功率、延迟、连续失败次数；也做了评测链路，用测试集真实跑 Agent，再用 LLM-as-Judge 从相关性、准确性、完整性、有用性几个维度评分。这样后续改 prompt、改路由或改工具逻辑时，可以发现有没有回归。

如果总结成一句话，我认为最难的是：**把大模型回答能力工程化成一个稳定的智能客服系统**。也就是让它不是单次聊天，而是能结合知识库、记忆、Skills、Agent 路由、监控和评测，形成一个可降级、可追踪、可持续优化的闭环。

### 9. 你这套逻辑本质上是不是还是关键词匹配？第一步算完主意图之后，第二步具体做什么？

不是简单的关键词匹配。我的设计是把“理解用户主要诉求”和“判断是否需要多 Agent 协作”拆成两步。

第一步先识别主意图，它解决的是用户这句话最主要要处理什么，比如是登录故障、退款、发票、物流，还是转人工。这个结果不是只靠关键词，而是结合大模型语义判断、模板相似度和规则兜底，最后输出一个主意图、置信度、意图组、紧急程度和一些结构化实体。

第二步不是重新做一次意图识别，而是做路由决策。它会把主意图先映射到一个默认处理方向，比如登录故障默认技术 Agent，退款和发票默认账单 Agent，物流咨询默认通用 Agent。然后系统会再看这句话里有没有其他领域的强信号，比如错误码、金额、订单号、退款、扣款、发票这类实体或关键词。

举个例子，用户说“登录一直报 401，而且刚才还重复扣款了”。主意图可能是登录故障，所以主处理 Agent 是技术 Agent。但第二步会发现里面还有“扣款”这种账单信号，于是给账单领域加分。如果账单分数达到阈值，就把账单 Agent 加成辅助 Agent，并行处理账务部分。

所以关键词在这里不是用来代替语义识别，而是用来做复合问题的补充召回。这样设计的取舍是：主意图保持单一，路由稳定、可评估；跨领域问题再用领域信号补齐，避免一个复杂问题只交给一个 Agent 导致漏答。

它的边界是，关键词权重和阈值需要调优。如果辅助领域分数太低，就可能漏掉第二个 Agent；如果太敏感，又会误触发多 Agent。所以这部分要结合评测集和线上样例持续校准。

### 10. 如果主意图都没命中，说明指令很模糊，这时候触发多 Agent 并行分析合理吗？

不合理。我的设计里，多 Agent 并行不是用来处理“完全模糊”的问题，而是用来处理“明确包含多个领域信号”的问题。

如果主意图没有命中，通常说明系统还不能判断用户到底要咨询订单、退款、技术故障还是账户问题。这时候如果直接拉多个 Agent 并行分析，表面上看覆盖面更广，但实际会带来两个问题：第一是成本和延迟变高；第二是多个 Agent 都在猜，最后合并出来的答案可能更啰嗦，甚至把用户没有表达的诉求强行展开。

所以我会把这类请求先分成两种情况。

第一种是真模糊，比如用户只说“帮我看看”“有问题”“处理一下”。这种情况应该先澄清，问用户具体是订单物流、退款账单、账户资料，还是技术故障，而不是直接多 Agent。

第二种是主意图低置信度，但消息里有很强的领域证据，比如出现了错误码、金额、订单号、发票、扣款等。这时可以用这些结构化信号做兜底路由，但也应该选择最有证据的一个主方向，而不是无差别并行。

所以我的边界是：多 Agent 只在“多个领域证据都比较明确”时触发；如果只是主意图没命中、用户表达模糊，优先追问澄清。这样能减少误路由，也能避免把简单问题复杂化。

### 11. 讲一下智能客服系统一次请求的链路，用了什么架构，从用户输入到最终回复中间都设计了哪些节点？

这套系统整体是一个分层的 Agent 编排架构，不是用户输入后直接丢给大模型。它的核心思路是：先理解问题，再决定要不要查知识库，再选择合适的 Agent，最后生成可控回复并写回记忆。

一次请求进来后，第一步先读取上下文，包括最近几轮对话、长期记忆和用户画像。这样模型不是只看当前一句话，而是能知道用户之前问过什么。

第二步做意图识别和结构化解析。系统会判断用户主要是在问物流、退款、发票、技术故障、账户问题，还是转人工，同时提取订单号、金额、错误码这类关键实体，并判断紧急程度。

第三步是知识库判断。不是所有问题都查知识库，比如问候和闲聊就不查；只有业务类问题才触发检索。检索时会做查询改写、并行召回和重排，把最相关的业务规则放进上下文，避免模型凭常识编退款政策或配送规则。

第四步进入 Agent 编排。系统会根据主意图、实体和领域信号计算路由结果，决定是交给通用 Agent、技术 Agent、账单 Agent，还是需要主 Agent 加辅助 Agent 协作。比如“登录 401 还重复扣款”，技术 Agent 主处理登录问题，账单 Agent 辅助处理扣款问题。

第五步是 Agent 生成回复。不同 Agent 会加载不同的业务规范和 Skills，比如技术 Agent 更关注排障步骤，账单 Agent 更关注退款审核和敏感信息保护。这样同一个大模型在不同场景下会受到不同规则约束。

最后系统会做结果处理，包括判断是否需要升级人工、把本轮用户消息和回复写入记忆、异步更新用户画像，同时监控 Agent 和工具的成功率、延迟和失败情况。

所以这套链路的价值是把大模型从“直接聊天”变成了“可路由、可检索、可降级、可观测”的客服系统。边界是链路变长后复杂度和延迟会上升，所以我做了按意图触发检索、失败降级和异步画像更新，尽量保证主链路稳定。

### 12. 通用 Agent 里包含售前、售后、物流、快递等问题，它里面是怎么区分的？是靠 Skill 吗？

通用 Agent 不是完全靠 Skill 区分，也不是把所有问题混在一起直接回答。我的设计是：专业风险比较高、处理规范差异明显的技术和账单单独拆 Agent；剩下的售前、售后、物流、订单咨询这类问题，先放在通用 Agent，但在进入通用 Agent 之前已经做了细粒度理解和知识增强。

具体来说，系统前面会先识别细粒度意图，比如物流、订单状态、普通咨询、投诉、请求操作等。这个结果会决定是否查知识库，以及查哪类业务规则。比如物流问题会召回配送规则，售后问题会召回退换货或处理流程，售前咨询会更偏产品和权益说明。

然后通用 Agent 拿到的不是一条裸用户输入，而是“用户问题 + 相关知识库内容 + 结构化实体 + 历史上下文”。所以它内部区分场景主要靠前置意图、知识库上下文和大模型语义理解共同完成。

Skill 在这里更像规则约束，而不是主分类器。比如通用客服 Skill 会约束回复风格、信息确认、什么时候建议转人工；如果后续有更细的物流 Skill、售后 Skill，也可以按关键词和 Agent 类型动态注入。但我不会把 Skill 当成唯一的区分方式，因为纯 Skill 或纯关键词很难覆盖复杂表达。

这样设计的收益是 Agent 数量不会一开始就膨胀，系统比较容易维护；同时通过细粒度意图和知识库，通用 Agent 也能处理不同业务场景。边界是如果某一类问题，比如物流或售后，复杂度和量级持续升高，就应该继续拆成独立 Agent 或更细粒度的专门 Skill。

### 13. 技术 Agent 单独拆出来后，它里面的 Skill 是固定 SOP，还是有多条链路？

它不是一个完全固定的单线 SOP，更像是一组技术支持场景下的动态处理规范。技术 Agent 本身负责技术类问题，Skill 负责把不同技术场景的排障要求注入进去。

我把技术问题拆成几类常见链路，比如登录失败、接口 500、401/403 认证授权问题、超时或连接失败、配置和部署问题。它们共用一个总原则：先确认现象，再判断影响范围，再收集必要信息，最后给出低风险排查步骤和验证方式。

但不同场景的侧重点不一样。比如 401 更关注 Token、API Key、签名和权限；500 更关注 request_id、接口路径、请求时间和服务端日志；超时更关注网络、DNS、防火墙、限流；部署问题更关注环境变量、端口、依赖版本和容器网络。

所以从实现上看，当前 Skill 不是一个显式的流程状态机，而是按关键词和 Agent 类型动态注入的一组分支化排障规范。模型会根据用户输入里的错误码、日志、环境信息，选择更适合的排查路径。

这样设计的收益是足够轻量，能覆盖大多数客服技术问题；边界是如果后续要做更强的自动诊断，比如自动查日志、跑健康检查、定位服务依赖，那就应该把这些 Skill 进一步升级成工具链驱动的多步骤 workflow。

### 14. Skill 实现的是固定 SOP 流程，还是相当于是检索知识库？

Skill 更接近 SOP 和行为规范，不是知识库检索。

我在系统里把 Skill 和知识库分开设计。知识库解决的是“事实是什么”，比如退款规则、配送时效、接口说明、产品政策，这些内容会根据用户问题动态检索，取最相关的片段放进上下文。

Skill 解决的是“Agent 应该怎么处理这个问题”。比如技术 Agent 的 Skill 会要求先确认现象、收集错误码和日志、按低风险步骤排查、不要让用户泄露密钥；账单 Agent 的 Skill 会要求注意退款审核、金额确认、隐私保护和人工升级条件。

所以它不是向量检索意义上的知识库，而是一组按 Agent 类型和关键词动态注入的处理规范。它有 SOP 的性质，但不是死板的一条流程。比如同样是技术问题，401 会走认证授权排查思路，500 会走服务端错误排查思路，超时会走网络和依赖服务排查思路。

这样设计的好处是边界清楚：知识库保证事实准确，Skill 保证处理方式稳定。边界是 Skill 本身不负责存大量业务事实，也不替代知识库；如果规则越来越多，就需要拆成更细的 Skill 或沉淀成可执行 workflow。

### 15. Skill 实现和调试过程中遇到过什么问题？哪些地方和设想有偏差，怎么解决的？

Skill 这块最大的偏差不是写规则本身，而是规则什么时候注入、注入多少、模型能不能稳定执行。

第一个问题是过度注入。一开始如果把所有客服规范都塞进 prompt，模型会变得啰嗦，也容易把不相关的规则带进回答。比如用户只是问物流，结果模型也提退款审核或技术排障，这会干扰体验。我的解决方式是按 Agent 类型和关键词动态注入，只让当前场景相关的 Skill 进入上下文。

第二个问题是规则冲突。比如通用客服要求回复简洁，技术支持又要求步骤完整；账单问题要求谨慎确认金额，投诉场景又要求先安抚情绪。如果这些规则没有边界，模型会不知道优先级。我的处理是把 Skill 拆成不同 Agent 的规范，并且让专业 Agent 的规范优先于通用规范。

第三个问题是关键词触发不够稳定。用户表达很口语，比如“不让我进去了”“页面炸了”“钱扣重了”，不一定刚好命中配置的关键词。这个问题不能只靠堆关键词解决，所以前面先做细粒度意图识别和实体提取，Skill 触发只是辅助信号。后续再通过评测样例补充高频表达。

第四个问题是 Skill 和知识库边界容易混。Skill 里如果写太多业务事实，就会出现规则过期的问题，比如退款时效、发票政策变了，Skill 还保留旧说法。我的原则是 Skill 只写处理方法和安全边界，具体政策放知识库，回答时优先依据检索到的业务文档。

调试上，我主要看三类结果：本轮到底命中了哪些 Skill、回答有没有遵守对应规范、有没有因为 Skill 注入导致回答变长或跑偏。然后用评测集覆盖典型场景，比如登录 401、接口 500、重复扣款、发票、退款、物流等，观察改 Skill 以后有没有回归。

所以总结一下，Skill 的难点是把规则做成“刚好够用的约束”，而不是越多越好。收益是能稳定约束不同 Agent 的行为；边界是 Skill 不能替代意图识别和知识库，也需要持续评测和迭代。

### 16. 最终回复是模型生成的，还是有模板？

正常业务回复是模型生成的，不是固定模板拼接。但它不是完全自由生成，而是在 Agent 角色、知识库内容、历史上下文、结构化实体和 Skill 规范共同约束下生成。

我没有把客服回答做成大量硬编码模板，是因为用户表达变化很大，同一个退款或技术问题也可能包含不同上下文。如果纯模板，覆盖成本会很高，也容易答得生硬。

但系统里确实有少量模板化兜底，比如低置信度时追问澄清、Agent 失败时返回统一错误提示、多 Agent 并行时按主处理和辅助处理合并结果。这些属于控制链路，不是主要的业务回答。

所以可以理解为：业务内容由模型生成，回复边界由知识库和 Skill 约束，异常和澄清场景用模板兜底。这样既保留大模型的自然表达能力，又能保证客服系统在关键场景下可控。

### 17. 技术问题可以让模型自由发挥，但账单场景通常有固定规则和话术。系统里有这种固定规则吗？

有。账单 Agent 的自由度会明显低于技术 Agent，因为它涉及资金、退款承诺、发票和用户隐私，不能只靠模型根据常识回答。

目前系统通过账单 Skill 固定了几类规则。第一类是必须核验的信息，比如订单号、支付时间、支付渠道、金额和交易记录；第二类是禁止性规则，比如不能在未查询系统前承诺退款成功、立即到账或认定重复扣款；第三类是标准处理路径，比如退款需要经过订单核验、审核和原路退回，发票作废或重开需要满足条件并可能转人工；第四类是升级条件，涉及实际退款、补偿、账单调整和异常扣款时必须交给人工或财务审核。

最终回复仍然由模型组织语言，但模型只能在这些规则边界内表达。对于低风险的解释性内容，可以保持自然表达；涉及金额、时效、审核结果和操作状态时，必须使用保守话术，不能生成确定性承诺。

不过当前实现主要还是通过 prompt 和 Skill 约束，还不是独立的业务规则引擎。真实生产环境里，我会进一步把退款资格、订单状态、到账时效和可执行操作交给确定性的规则引擎或业务系统判断，关键话术使用结构化模板，模型只负责解释结果和组织语言。

这样设计的收益是既保留自然交互，又不会把资金类决策交给大模型。边界是 Skill 可以约束表达，但不能替代真实订单查询、审批流程和财务系统。

### 18. 如果用户的问题同时涉及技术和账单，是怎么路由的？

这种复合问题不会强行只选一个 Agent。我的做法是先识别一个主意图确定主处理方向，再通过领域打分判断是否需要增加辅助 Agent。

比如用户说“登录一直报 401，而且刚才还重复扣款了”。第一步可能把主意图识别为登录故障，因此技术 Agent 是默认主处理方向。第二步会综合主意图、技术和账单关键词，以及错误码、金额等结构化实体，分别计算技术领域和账单领域的分数。

分数最高的 Agent 成为主 Agent。如果另一个专业领域也有足够明确的证据，并且分数达到辅助 Agent 门槛，就会加入辅助 Agent。这样这个请求会同时发给技术 Agent和账单 Agent：技术 Agent负责登录和 401 排查，账单 Agent负责重复扣款核验，两边并行执行，最后按主处理和辅助处理合并回复。

如果账单信号很弱，只是历史背景或者偶然提到，就不会自动拉起账单 Agent，避免过度路由。如果涉及实际退款、异常扣款或需要后台操作，账单 Agent 也只能给出核验路径，并继续升级人工或财务处理。

这样设计的收益是既保持主次关系，又避免单主意图漏掉第二个业务问题。边界是辅助领域的权重和阈值需要通过复合问题评测集持续校准。
